define([
  'factory/radian-factory'
], function(RF) {
  RF('stubFactory', [
    '$rootScope'
  ], function($rootScope) {
    var factory,
        privateMethod;

    privateMethod = function() {

    };

    return factory = {
      publicMethod: function() {

      }
    };
  });
});